// import { defineConfig } from 'dumi';

export default {// defineConfig({
  title: 'common-hooks',
  history: { type: 'hash' },
  favicon: './luigi.ico',
  logo: './luigi.ico',
  resolve: {
    includes: ['example'],
  },
  publicPath: './',
  outputPath: 'docs',
  // more config: https://d.umijs.org/config
};
